{
	"name": "raw",
	"module": "index.ts",
	"devDependencies": {
		"@types/bun": "1.1.9"
	},
	"peerDependencies": {
		"typescript": "5.0.0"
	},
	"scripts": {
		"devraw": "bun run raw.ts",
		"devunraw": "bun run unraw.ts",
		"build": "bun build raw.ts --compile --outfile raw-ts.exe && bun build unraw.ts --compile --outfile unraw-ts.exe"
	},
	"type": "module"
}
