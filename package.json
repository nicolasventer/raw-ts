{
	"name": "raw",
	"module": "index.ts",
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5.0.0"
	},
	"scripts": {
		"dev": "bun run index.ts",
		"build": "bun build index.ts --compile --outfile raw-ts.exe"
	},
	"type": "module"
}
